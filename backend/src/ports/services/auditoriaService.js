/**
 * @interface AuditoriaService
 * 
 * @method registrar - (details: { usuario_id: string, username: string, accion: string, modulo: string, detalle: Object, ip_address: string }) => Promise<void>
 */

module.exports = {};
