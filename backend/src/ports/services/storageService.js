/**
 * @interface StorageService
 * 
 * @method upload - (file: { buffer: Buffer, name: string, mimetype: string, size: number }, path: string) => Promise<{ filePath: string, publicUrl: string }>
 * @method remove - (path: string) => Promise<void>
 */

module.exports = {};
